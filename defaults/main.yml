---

nova_conductor_hostname: "{{ ansible_eth0.ipv4.address }}"

mysql_hostname: localhost
rabbit_hostname: localhost

mysql_admin_username: root
mysql_rootpass: mysql_root_default

rabbit_username: rabbit_username_default
rabbit_pass: rabbit_pass_default

nova_conductor_dbpass: nova_conductor_dbpass_default
